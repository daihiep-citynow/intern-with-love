import HeaderLogoComponent from ".";

export default {
  title: "Layouts/Header/Header Components/Header Logo",
  component: HeaderLogoComponent,
};

const Template = () => <HeaderLogoComponent />;

export const HeaderLogo = Template.bind({});
