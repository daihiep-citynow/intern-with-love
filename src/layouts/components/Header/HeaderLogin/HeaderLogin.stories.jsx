// components
import HeaderLoginComponent from ".";

export default {
  title: "Header Login",
  component: HeaderLoginComponent,
};

const Template = () => <HeaderLoginComponent />;

export const HeaderLogin = Template.bind({});
