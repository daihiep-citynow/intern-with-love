import FooterInfoComponent from ".";

export default {
  title: "Footer Info",
  component: FooterInfoComponent,
};

const Template = () => <FooterInfoComponent />;

export const FooterInfo = Template.bind({});
